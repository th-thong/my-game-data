root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/api/main.go"
  bin = "tmp/main"
  delay = 1000
  exclude_dir = ["assets", "tmp", "vendor", "mongodb_data"]
  include_ext = ["go", "tpl", "tmpl", "html", ".env"]

[log]
  time = true